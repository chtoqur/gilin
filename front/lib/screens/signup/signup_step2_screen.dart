import 'package:flutter/material.dart';

class SignupStep2Screen extends StatelessWidget {
  const SignupStep2Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('SignUp Step2 Screen'),
    );
  }
}
