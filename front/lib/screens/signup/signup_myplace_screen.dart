import 'package:flutter/material.dart';

class SignupMyplaceScreen extends StatelessWidget {
  const SignupMyplaceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Signup Myplace Screen'),
    );
  }
}
